const game = new Phaser.game({
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  scene: {
    preload: preload,
    create: create,
    update: update,
  },
});

function preload() {
  // do work
}

function create() {
  // do work
}

function update() {
  // do work
}
